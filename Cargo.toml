[package]
name = "energy-plots"
version = "0.1.0"
edition = "2021"
description = "A Rust DSL for generating clean PGFPlots/TikZ code from CSV energy measurement data"
license = "MIT"

[dependencies]
csv = "1"
